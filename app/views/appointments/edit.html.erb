<% content_for :title, "Editing appointment" %>

<h1>Editing appointment</h1>

<%= render 'form', appointment: @appointment, patient: @patient %>

<br>

<div>
  <%= link_to "Show this appointment", patient_appointment_path(@patient, @appointment) %> |
  <%= link_to "Back to patient", patient_path(@patient) %>
</div>
