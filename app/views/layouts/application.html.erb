<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Patient Treatment Plans" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-gray-50 text-gray-800 font-sans leading-relaxed antialiased">
    <div class="bg-blue-200 p-4 rounded-lg shadow-lg">
      </br>
      </br>
    </div>
    <% if user_signed_in? %>
      <nav class="bg-white border-b border-gray-200 shadow-sm">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between h-16 items-center">
            <div class="flex space-x-8">
              <%= link_to "Patients", root_path, class: "inline-flex items-center px-3 py-2 text-sm font-medium text-gray-700 hover:text-blue-600 hover:border-b-2 hover:border-blue-600 transition" %>
              <%= link_to "Appointments", all_appointments_path, class: "inline-flex items-center px-3 py-2 text-sm font-medium text-gray-700 hover:text-blue-600 hover:border-b-2 hover:border-blue-600 transition" %>
              <%= link_to "Exercises", exercises_path, class: "inline-flex items-center px-3 py-2 text-sm font-medium text-gray-700 hover:text-blue-600 hover:border-b-2 hover:border-blue-600 transition" %>
            </div>
            <div class="flex items-center">
              <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "text-sm text-red-600 hover:text-red-800 bg-white border border-red-600 px-3 py-1 rounded transition", form_class: "inline" %>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <main class="max-w-7xl mx-auto px-4 py-6 sm:px-6 lg:px-8">
      <%= yield %>
    </main>
  </body>

</html>
